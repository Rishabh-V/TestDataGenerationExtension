# TestDataGenerationExtension
Visual Studio Extension  that provides a UI for your models/entities to provide test data/values to the properties. The data can then be serialised to JSON or XML as needed.
